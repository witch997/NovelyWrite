# NovelyWrite 打包分发指南

## 运行方式

NovelyWrite 采用「代码根 / 数据根」分离，支持三种运行方式：

### 方式 1：本机默认（数据根 = 代码根）

```
直接运行：node cli.mjs <task>
路径：config.json / corpus / store 都在 NovelyWrite/ 下
```

### 方式 2：分发（NOVELYWRITE_HOME 指定数据根）

```
设置环境变量：
  NOVELYWRITE_HOME=/data/lvshi        # 数据根（config/corpus/store 所在）
  NOVELYWRITE_CHAT_API_KEY=sk-xxx     # 对话 API key（可选，覆盖 config.json）
  NOVELYWRITE_EMBED_API_KEY=sk-xxx    # embedding API key（可选）
  NOVELYWRITE_CHAT_BASE_URL=...       # 可选，覆盖 baseUrl
  NOVELYWRITE_EMBED_BASE_URL=...      # 可选

运行：node cli.mjs <task>
路径：
  代码（只读）：<包>/novelread/ <包>/retriever/ ...
  数据（可写）：$NOVELYWRITE_HOME/config.json、$NOVELYWRITE_HOME/corpus/、$NOVELYWRITE_HOME/store/
```

### 方式 3：Web 工作台（server.mjs + webview 前端）

```
启动：node server.mjs [--port=3081] [--host=127.0.0.1] [--open]
  --port=0   → 动态端口（系统分配，杜绝冲突；推荐）
  --open     → 服务就绪后自动打开浏览器

访问：http://127.0.0.1:<端口>  → webview 工作台（三栏：大纲/编辑器/AI 参考）
接口：/api/projects /api/search /api/config /api/sessions /api/tasks/*

注意：默认端口 3081，不要用 3080（DeepSeek Harness Web GUI 占用）。
```

## 关键设计

### 路径模块（shared/paths.mjs）
```
CODE_ROOT：代码根（包内，只读）
DATA_ROOT：数据根（NOVELYWRITE_HOME > 默认=CODE_ROOT）
configPath / corpusDir / storeDir / mybookDir / myprojectDir / exprojectDir
projectRoot()（域感知）/ createProject()（禁止同名）/ ensureDataDirs()
```

### 域化布局（store 下两域 + 资产区）
```
store/myproject/<书>project/    我的作品（用户自己的书）
store/exproject/<书>project/    外部知识库（语料标注的书）
store/<域>/<书>project/derived/ 每书独立派生（dict/vector，英文目录）
mybook/<书>/                   用户原稿（数据根，不可再生资产）
```

### 配置模块（shared/config.mjs）
```
loadChatConfig(moduleName, featureDir) → chat 段（模块作用域覆盖）
loadEmbedConfig()                      → embed 段（就绪检测）
环境变量：NOVELYWRITE_CHAT_API_KEY / NOVELYWRITE_EMBED_API_KEY 等
功能层独立模型：根 config.json 的 features.shot-writing.chat（模块作用域，无独立 config 文件）
```

### 不用 DSH_* 变量
DSH_HOME / DSH_* 被 harness 环境占用，本项目统一用 NOVELYWRITE_* 前缀。

## API key 安全

- config.json 可留空 key（`"apiKey": ""`）
- 运行时用环境变量注入：`NOVELYWRITE_CHAT_API_KEY` / `NOVELYWRITE_EMBED_API_KEY`
- 这样打包分发时 config.json 不含敏感信息
- 功能层模型等参数统一在根 config.json 的 features 段（模块作用域），无独立 config 文件

## 子进程调用（execFileSync）

- cli / host-exec / aggregates / fix 用 `execFileSync(process.execPath, [模块路径, ...])` 调子模块
- 子进程继承环境变量（NOVELYWRITE_HOME / NOVELYWRITE_* 自动传给子进程）
- 模块路径用 `__dirname` 定位（代码根内，打包后随包分发）
- server.mjs 用 `spawn` 起长任务子进程（标注/聚合/写作），状态+日志落 store/_tasks/

## 分发目录结构（建议）

```
NovelyWrite/
├── cli.mjs                # CLI 入口
├── server.mjs             # Web 服务入口（node:http 内置，零依赖）
├── webview/               # 前端（index.html/app.js/style.css/vendor/vditor）
├── novelread/             # 代码（只读）
├── retriever/             # 代码
├── shared/                # 代码
├── features/              # 代码
└── （数据目录由 NOVELYWRITE_HOME 指定，或运行时创建）
```

目标机需安装 Node.js（>=18，fetch 可用）。

## 前端（webview/）说明

- 零构建：纯 HTML/CSS/JS + Vditor（本地 vendor/，不引 CDN）
- Vditor 用于 Markdown 编辑器（本地化，SEA/Tauri 打包需本地资源）
- server.mjs 静态托管 webview/ 目录（/ → index.html，/vendor/* → 本地文件）

## SEA 打包（规划）

- 目标：单 exe（内嵌 Node + server + webview），目标机免装 Node
- 前置改造：① 子进程 execFileSync → 进程内函数调用 ② sessions/state 迁数据根 ③ 静态资源内联
- 当前状态：讨论中，未实施
